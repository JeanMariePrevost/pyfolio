{% extends "base.jinja" %}

{% block content %}
    <h1>Gallery</h1>
    <ul>
        {% for element in elements %}
            <li>
                <a href="{{ element.get_element_page_url() }}">{{ element.get_file_name_without_extension() }}</a>
                {# TODO: make the asset text into a tooltip? A trimmed, no-formatting version of it? #}
            </li>
        {% endfor %}
    </ul>
{% endblock content %}
